{
  "scripts": {
    "build": "tsc --outDir dist",
    "test": "tsc --outDir dist && cd dist && node example_1.js"
  },
  "dependencies": {
    "puppeteer": "^21.7.0",
    "typescript": "^5.3.3"
  }
}
